{
  "name": "player",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "generate-palettes": "node scripts/generate-palettes.js",
    "prebuild:music": "node scripts/optimize-music.js",
    "prebuild:img": "node scripts/optimize-images.js",
    "prebuild:assets": "node scripts/copy-assets.js",
    "prebuild": "npm run generate-palettes && npm run prebuild:music && npm run prebuild:img && npm run prebuild:assets",
    "dev": "vite",
    "build": "npm run prebuild && vite build",
    "preview": "vite preview --port 8080",
    "clean": "node scripts/clean.js"
  },
  "keywords": [],
  "author": "Jonny Pyper / Carbontype / Music copyright belongs to respective owners",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "music-metadata": "^11.9.0",
    "node-vibrant": "^4.0.3",
    "sass-embedded": "^1.99.0",
    "sharp": "^0.34.4",
    "vite": "^8.0.13"
  },
  "dependencies": {
    "line-awesome": "^1.3.0"
  }
}
